package output.states

//TODO: seems like I could remove this entire class as it's a one-to-one to [OutputState]
enum class State(val outputState: OutputState) {
    PAPER_MOVING_FORWARD(OutputState.PAPER_MOVING_FORWARD),
    FORWARD_FIRST_LINE_STARTED(OutputState.PAPER_MOVING_FORWARD),
    FORWARD_FIRST_LINE_ENDED(OutputState.PAPER_MOVING_FORWARD),
    FORWARD_SECOND_LINE_STARTED(OutputState.PAPER_MOVING_FORWARD),
    FORWARD_SECOND_LINE_ENDED(OutputState.PAPER_MOVING_FORWARD),
    PAPER_MOVING_BACKWARD(OutputState.PAPER_MOVING_BACKWARD),
    CUT_TOWARDS_END(OutputState.CUTTING_TOWARDS_END),
    PAUSE_BEFORE_CUTS(OutputState.FULL_STOP),
    CUT_TOWARDS_START(OutputState.CUTTING_TOWARDS_START),
    STOP(OutputState.FULL_STOP),
    STOP_WITH_ELECTROMAGNET_GOING_DOWN(OutputState.MOVING_HOLDING_MOTOR_DOWN),
    STOP_WITH_ELECTROMAGNET_GOING_UP(OutputState.MOVING_HOLDING_MOTOR_UP),
    BACKWARD_ELECTROMAGNET_DOWN(OutputState.MOVING_BACKWARD_WITH_ELECTROMAGNET_DOWN),
}